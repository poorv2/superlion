var Schema = require("@truffle/contract-schema");

var Contract = require("./lib/contract");

var truffleContractVersion = require("./package.json").version;

var contract = function contract() {
    var json = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
    var normalizedArtifactObject = Schema.normalize(json); // Note we don't use `new` here at all. This will cause the class to
    // "mutate" instead of instantiate an instance.

    return Contract.clone(normalizedArtifactObject);
};

contract.version = truffleContractVersion;
module.exports = contract;

if (typeof window !== "undefined") {
    window.TruffleContract = contract;
}